import 'package:flutter/material.dart';

class AppColors {
  // Premium B2B Palette
  static const primary = Color(0xFF2563EB); // Deep Enterprise Blue
  static const primaryLight = Color(0xFF60A5FA);
  static const primaryLighter = Color(0xFFDBEAFE);
  static const secondary = Color(0xFF0F172A); // Slate Navy
  static const accent = Color(0xFFF59E0B); // Amber for alerts

  // Backgrounds
  static const backgroundPrimary = Color(0xFFFFFFFF);
  static const backgroundSecondary = Color(0xFFF1F5F9);
  static const backgroundTertiary = Color(0xFFE2E8F0);

  static const surface = Color(0xFFFFFFFF);
  static const border = Color(0xFFE2E8F0);
  static const divider = Color(0xFFF1F5F9);

  // Status Colors (B2B refined)
  static const success = Color(0xFF059669); // Emerald
  static const successLighter = Color(0xFFD1FAE5);
  static const error = Color(0xFFDC2626); // Red
  static const errorLighter = Color(0xFFFEE2E2);
  static const warning = Color(0xFFD97706); // Amber
  static const warningLighter = Color(0xFFFEF3C7);
  static const info = Color(0xFF0284C7); // Sky Blue
  static const infoLighter = Color(0xFFE0F2FE);

  // Typography
  static const textPrimary = Color(0xFF0F172A); // Slate 900
  static const textSecondary = Color(0xFF334155); // Slate 700
  static const textLight = Color(0xFF64748B); // Slate 500
  static const textDisabled = Color(0xFFCBD5E1); // Slate 300

  static const shadow = Color(0x0F000000);
  static const shadowDeep = Color(0x1A000000);
}
